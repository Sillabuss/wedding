# wedding.frontend

Angular single page site for Caterina and Massimiliano's wedding.

## Scripts

- `npm start` - run the local dev server
- `npm run build` - create a production build
- `npm run watch` - build in watch mode

## Structure

- `src/app/wedding/` contains the only page component.
- `public/` contains the images used by the page.
